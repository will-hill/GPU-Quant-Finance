[project]
name = "gpu-options-scanner"
version = "0.1.0"
description = "Continuous American-options scanner -- live Theta Data, IV + 6 Greeks on GPU"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cupy-cuda13x>=14.1.1",
    "jupyter>=1.1.1",
    "matplotlib>=3.11.0",
    "numpy>=2.0",
    "polars>=1.20",
    "python-dotenv>=1.2.2",
    "quantlib>=1.43",
    "thetadata>=1.0.9",
    "torch>=2.13.0",
]
